# name: Snowflake Deploy
# on:
#   push:
#     branches: [ main ]

# permissions:
#   id-token: write
#   contents: read

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Install Snowflake CLI and Fix Compatibility
#         run: |
#           pip install snowflake-cli "pyOpenSSL>=22.1.0"

#       - name: Write private key to file
#         run: |
#           echo "$SNOWFLAKE_PRIVATE_KEY" > private_key.p8
#         env:
#           SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}

#       - name: Run Deploy Script
#         env:
#           SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
#           SNOWFLAKE_USER: GITHUB_SERVICE_USER
#           SNOWFLAKE_AUTHENTICATOR: SNOWFLAKE_JWT
#           SNOWFLAKE_PRIVATE_KEY_PATH: private_key.p8

#         run: |
#           # Test the connection first
#           snow sql -q "SELECT CURRENT_VERSION()" -x

#           # Deploy your actual table
#           snow sql --filename scripts/create_table.sql -x
name: Snowflake Deploy
on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # IMPORTANT: needed for git diff

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Snowflake CLI
        run: |
          pip install snowflake-cli "pyOpenSSL>=22.1.0"

      - name: Write private key to file
        run: |
          echo "$SNOWFLAKE_PRIVATE_KEY" > private_key.p8
        env:
          SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}

      - name: Determine changed SQL files
        id: changes
        run: |
          git remote set-url origin https://github.com/${{ github.repository }}.git
          git fetch origin main

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "Detecting changes in PR..."
            CHANGED_FILES=$(git diff --name-only origin/main...HEAD | grep -E '\.sql$' || true)
          else
            echo "Detecting changes on push to main..."
            CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD | grep -E '\.sql$' || true)
          fi

          echo "Changed SQL files:"
          echo "$CHANGED_FILES"

          # Convert newline-separated list into space-separated list
          SAFE_FILES=$(echo "$CHANGED_FILES" | tr '\n' ' ')

          echo "files=$SAFE_FILES" >> $GITHUB_OUTPUT

      - name: Run changed SQL scripts
        if: steps.changes.outputs.files != ''
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: GITHUB_SERVICE_USER
          SNOWFLAKE_AUTHENTICATOR: SNOWFLAKE_JWT
          SNOWFLAKE_PRIVATE_KEY_PATH: private_key.p8
        run: |
          echo "Running changed SQL files..."

          for file in ${{ steps.changes.outputs.files }}; do
            echo "Executing: $file"
            snow sql --filename "$file" -x
          done

      - name: No SQL changes detected
        if: steps.changes.outputs.files == ''
        run: echo "No SQL files changed. Skipping deployment."